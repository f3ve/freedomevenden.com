export type PageFrontmatter = {
  path: string;
  title?: string;
  subtitle?: string;
  description?: string;
  datePublished?: string;
  dateUpdated?: string;
  image?: string;
  /** Role I had on a project */
  role?: string;
};
